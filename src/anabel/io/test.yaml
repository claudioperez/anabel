#include <header.h>
#define

Vars:
- {( A:=1/3 )}
- {( b:=bytes(2))}

Model: # comment
  xyz:
    - [0.0,  {L}]
    - [0.1, {L*2}]
    - {  [   x for x in [0.3, 0.2, 0.5] ] }
    - { set( x for x in [20., 20., 200] ) }
  el:
    f:
      E: {E!r}
      A: {A:0.3}
      I: {
        [ i for i in [0, 1, '3'] ]
      }


