gci -r -i *.md |foreach{$docx=$_.directoryname+"\"+$_.basename+".docx";pandoc -f markdown+smart -s $_.name -o $docx}


gci -r -i *.md |foreach{$tex=$_.directoryname+"\"+$_.basename+".tex";pandoc -f markdown+smart -s $_.name -o $tex}

## Make .tex files without a preamble from every .md file in current directory:
gci -r -i *.md |foreach{$tex=$_.directoryname+"\"+$_.basename+".tex";pandoc -f markdown+smart $_.name -o $tex}

